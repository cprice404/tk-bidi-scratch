global {
    logging-config = ./dev-resources/logback-dev.xml
}

webserver {
    host = localhost
    port = 8080
}

web-router-service: {
    "tk-bidi-scratch.tk-bidi-scratch-web-service/hello-web-service": "/hello"
}
